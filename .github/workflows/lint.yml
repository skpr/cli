name: Lint

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v5

      - name: Install Mise
        run: |
          curl https://mise.run | sh
          mise install

      - name: Lint
        run: |
          mise run lint
