package ssh

import (
	"golang.org/x/crypto/ssh"
)

// Helper function to update the height and width on window resize.
// @todo, Provide support for window resize in Windows.
func monitorTerminalSizeChange(session *ssh.Session) {
	return
}
